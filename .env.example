
# -----------------------------------------------------------------------------
# PRIVATE ENVIRONMENT VARIABLES (Server-only)
# -----------------------------------------------------------------------------
# These variables are only available on the server-side and are secure
# Import with: import { VARIABLE_NAME } from '$env/static/private'

# EmailJS Configuration
# Get these values from your EmailJS dashboard (https://www.emailjs.com/)
EMAILJS_SERVICE_ID=your_service_id_here
EMAILJS_TEMPLATE_ID=your_template_id_here
EMAILJS_PUBLIC_KEY=your_public_key_here
EMAILJS_PRIVATE_KEY=your_private_key_here

# -----------------------------------------------------------------------------
# PUBLIC ENVIRONMENT VARIABLES (Client + Server)
# -----------------------------------------------------------------------------
# These variables are available on both client and server
# Import with: import { PUBLIC_VARIABLE_NAME } from '$env/static/public'
# NOTE: These will be visible in the browser, so never put sensitive data here!

# Example public variables (uncomment if needed):
# PUBLIC_SITE_NAME=Matthias Bigl Portfolio
# PUBLIC_CONTACT_EMAIL=biglmatthias@gmail.com
# PUBLIC_GITHUB_USERNAME=matthiasbigl
